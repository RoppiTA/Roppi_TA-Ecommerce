export const DefaultCliente = () => {
  return (
    <div className="flex-1 flex items-center justify-center">
      <p className="text-lg font-medium text-brand-muted">
        Bienvenido al panel de cliente (si ves esta pantalla, es que la ruta no existe o aún no se ha implementado).
      </p>
    </div>
  );
};

export default DefaultCliente;
